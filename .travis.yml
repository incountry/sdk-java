addons:
  sonarcloud:
    organization: "incountry"

language:
  java

jdk:
  - openjdk8

sudo: false

notifications:
  email: false

script:
  - gradle wrapper --gradle-version=6.3
  - gradle build --rerun-tasks
  - gradle sonarqube